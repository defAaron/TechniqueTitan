# Streamlit Community Cloud — pinned for Linux + Python 3.11.
# Do NOT include "." here; app.py adds src/ to sys.path instead.
# mediapipe is pinned below 0.10.30: newer wheels removed mp.solutions.
mediapipe==0.10.21
opencv-python-headless==4.10.0.84
numpy>=1.26,<2
PyYAML>=6.0
streamlit>=1.30
