# Production / Streamlit Cloud dependencies.
# mediapipe is pinned below 0.10.30: newer wheels removed the legacy
# `mp.solutions` API that the hand detector relies on.
mediapipe==0.10.21
opencv-python-headless>=4.8,<5
numpy>=1.26,<2
PyYAML>=6.0
streamlit>=1.30
.
