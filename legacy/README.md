# Legacy

Archived files from the original project layout before the directory
restructure. Kept for reference and git history — do not use for new work.

| Path | Origin |
|---|---|
| `pycharm/HandTrackingMin.py` | Original upload via PyCharm (`PycharmProjects/HandTrackingProject/`) |
| `pycharm/HandTrackingModule.py` | Original upload via PyCharm (`PycharmProjects/HandTrackingProject/`) |

Current equivalents live in `scripts/` (renamed prototypes) and
`src/technique_titan/` (production modules).
