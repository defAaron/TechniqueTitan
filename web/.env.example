# API origin for production builds (no trailing slash).
# Local dev: leave empty — Vite proxies /v1 → http://127.0.0.1:8000
# Production (Vercel): https://technique-titan-api.onrender.com
VITE_API_BASE_URL=
